# Release Notes

## ✨ 本次更新亮点
- 配置模型升级：拆分为 `data_source` / `account` / `app` 三个配置域，并兼容旧配置写法
- CLI 结构化 JSON 输出进一步统一，便于 AI Agent 和自动化工具集成
- UI 信息架构优化，增加图标、提示文案和更清晰的操作分区
- 同步更新功能状态、使用说明、架构文档和 AI 开发文档

## 📦 产物
- `ashare-strategy-windows-x86_64.zip`
- 源码包

## 🙌 使用建议
- 普通用户继续使用默认文本模式和图形界面
- 自动化用户优先使用 `--output json`
- 老配置文件仍可继续使用，但建议逐步迁移到新的分层配置格式
